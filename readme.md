# Instalación:

```
 npm install express
 npm install mongoose
 npm install cors
 npm install dotenv
```

# Configuracion de las variables de entorno:

Crear un archivo .env en la raiz del backend con los siguientes datos:
```
MONGO_URI = [URI de nuestro cluster de Atlas]
PORT = 3000
```